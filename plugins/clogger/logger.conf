[logger]
# A path to the logging root (with the style.css file copied from the directory) from the bot root directory with trailing slash
path=/srv/http/archlinux.org.il/linux-il/publiclogs/
autostart=on
